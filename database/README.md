# Database Notes

- Source of truth schema: `schema.sql`
- Recommended ORM migrations: Alembic in backend pipeline
- Use PostgreSQL JSONB for flexible AI metadata, tender summaries, and dynamic addresses
- Add partitioning or warehouse replication when analytics volume grows significantly

